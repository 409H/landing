landing:
  build: ./
  restart: always
  ports:
    - "9000:9000"
  environment:
    VIRTUAL_PORT: "9000"
    VIRTUAL_HOST: "metamask.io"
    LETSENCRYPT_HOST: "metamask.io"
    LETSENCRYPT_EMAIL: "admin@metamask.io"